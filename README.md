# Neural-Network-with-Evolutionary-Optimization
This project implements a multi-class weather classification model using a feedforward neural network, where network weights are optimized using a Genetic Algorithm instead of traditional gradient-based backpropagation.
